- clean: ['~']

- link:
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.gitconfig-spotify: gitconfig-spotify
    ~/.gitignore-global: gitignore-global
    ~/.zshrc: zshrc
    ~/.ssh/config: ssh-config

